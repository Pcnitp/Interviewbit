public class Solution {
    
    public int solve(int n) {
        n = Math.abs(n);
        int i=0;
        int sum=0;
        
        while(sum<n || (sum-n)%2!=0){
            i++;
            sum+=i;
            if(sum==n) return i;
        }
        return i;
    }
}
